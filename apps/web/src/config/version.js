// App version metadata, injected at build time by Vite (see vite.config.js).
// APP_VERSION comes from package.json; APP_COMMIT and BUILD_DATE are computed
// on each build, so the footer always reflects exactly what's deployed.
export const APP_VERSION = __APP_VERSION__;
export const APP_COMMIT = __APP_COMMIT__;
export const BUILD_DATE = __BUILD_DATE__;
